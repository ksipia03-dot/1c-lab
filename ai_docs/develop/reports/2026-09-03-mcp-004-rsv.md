# MCP-004: MCP RSV Data on ERP25_MCP

**Date:** 2026-09-03  
**Orchestration:** orch-mcp-free-stack-20260827  
**Status:** Partial — artifacts ready, runtime blocked on training platform COM

## Done

| Step | Result |
|------|--------|
| Download MCP-RSV-Data v1.3.0 | OK → `C:\1C-Lab\tools\mcp-rsv-data\MCP-RSV-Data\` |
| Files | `RSVData.cfe`, `rsvdata-bridge.exe`, `guide.html` |
| Bridge config | `%APPDATA%\MCP-RSV-Data\rsv-data.json` (file IB, user Администратор) |
| `.cursor/mcp.json` | `rsv-data` stdio entry added (see project root) |

## Blockers

1. **COM connector missing** on educational platform `1cv8t` — no `comcntr.dll` under `C:\Program Files (x86)\1cv8t\8.3.27.1688`.  
   `rsvdata-bridge ping/diag` fails: `Invalid class string` / V83.COMConnector.

2. **Extension RSVData.cfe** — headless DESIGNER `/Extension /LoadCfg` did not produce a clear log; verify in Конфигуратор → Расширения → загрузить `RSVData.cfe` → F7.

## Workarounds (pick one)

- **A.** Install full platform 8.3.27 (not `1cv8t`) with COM, then `rsvdata-bridge ping`.
- **B.** Enable IIS, publish `ERP25_MCP`, use HTTP MCP (`streamable-http` → `.../hs/rsvdata/mcp`) — no COM bridge.
- **C.** Manual: run `rsvdata-bridge.exe` setup wizard once (GUI), it registers config and patches `mcp.json`.

## Next

After extension + COM or HTTP: smoke `ping`, `config`, `describe` via Cursor MCP tools.
