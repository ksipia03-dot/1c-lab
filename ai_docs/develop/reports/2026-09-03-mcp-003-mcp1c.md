# MCP-003: mcp-1c Open on ERP25_MCP

**Date:** 2026-09-03  
**Orchestration:** orch-mcp-free-stack-20260827  
**Infobase:** `C:\1C-Lab\Bases\ERP25_MCP`  
**Platform:** `C:\Program Files (x86)\1cv8t\8.3.27.1688\bin\1cv8t.exe`

## Summary

| Step | Result |
|------|--------|
| Copy `mcp-1c.exe` to `C:\1C-Lab\tools\mcp-1c\` | OK (16178176 bytes, v1.18.0; copy skipped when binary locked by index build) |
| `mcp-1c.exe --help` | OK |
| `-install` on ERP25_MCP | OK with `-db-user Администратор` (re-run 2026-09-03; exit 0) |
| Config dump | OK at `C:\1C-Lab\erp-dump\config\` (`Configuration.xml`, ~44k files); no CLI dump export — use DESIGNER `/DumpConfigToFiles` |
| `-build-index` | Cache present under `C:\1C-Lab\tools\mcp-1c\cache` (~64 MB); long-running rebuild started during verification |
| HTTP `-base` | **Not configured** — IIS/W3SVC not available; **dump-only** MCP (COM/HTTP live queries unavailable without web publish) |
| `.cursor/mcp.json` | Created — dump-only, `--dump` → `erp-dump\config` |
| `.dev.env` `INFOBASE_PATH` | Already `C:\1C-Lab\Bases\ERP25_MCP` |

## Extension install

MCP extension installed into ERP25_MCP. Role `MCP_ОсновнаяРоль` declared as default extension role; use access profiles on BSP bases (see install stderr).

## MCP client config

```json
{"mcpServers":{"1c-mcp":{"command":"C:\\1C-Lab\\tools\\mcp-1c\\mcp-1c.exe","args":["--dump","C:\\1C-Lab\\erp-dump\\config"]}}}
```

## Follow-ups

- MCP-004+: RSV Data, 1c_mcp, platform help, smoke.
- If HTTP publish is enabled later, add `-base` (and `-user`/`-password`) to `mcp.json` args.
