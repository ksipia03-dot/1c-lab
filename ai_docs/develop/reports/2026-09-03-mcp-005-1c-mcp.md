# MCP-005: 1c_mcp + mcp_dev on ERP25_MCP

**Date:** 2026-09-03  
**Orchestration:** orch-mcp-free-stack-20260827  
**Infobase:** `C:\1C-Lab\Bases\ERP25_MCP`  
**Platform:** `C:\Program Files (x86)\1cv8t\8.3.27.1688\bin\1cv8t.exe`  
**Status:** Partial — repo/proxy ready; extensions loaded headless (verify in Designer); runtime blocked without HTTP publish / 1C httppoll agent

## Done

| Step | Result |
|------|--------|
| Fetch `vladimir-kharin/1c_mcp` | OK → `C:\1C-Lab\tools\1c_mcp\` (GitHub `main.zip`; git not on PATH) |
| Build CFE | `build\MCP_Сервер.cfe` (+ ASCII copy `build\MCP_Server.cfe` for CLI) |
| `mcp_dev.cfe` | Used from `C:\Users\Administrator\Downloads\mcp_dev.cfe` |
| DESIGNER `/Extension /LoadCfg` | Run for both CFEs as `Администратор`; ~90–96 s each; logs `C:\1C-Lab\tools\1c_mcp\logs\` (Designer exit code 1 on 1cv8t — same quirk as MCP-004 RSV) |
| Python proxy | venv at `1c_mcp\venv`, `pip install -r src\py_server\requirements.txt` OK |
| Stdio smoke | `MCP_TRANSPORT=httppoll` → stdio MCP starts; listener on `127.0.0.1:9090` (agent 1C not verified) |
| `.cursor/mcp.json` | Added `1c-mcp-engine` (stdio + httppoll); kept existing `1c-mcp` dump server |

## Blockers (same class as MCP-003 / MCP-004)

1. **IIS / web publish disabled** — no HTTP URL for `.../hs/mcp/` (`MCP_TRANSPORT=http` path documented below, not usable yet).
2. **End-to-end tools** — httppoll proxy runs, but needs MCP extensions active in IB and 1C-side polling agent (typically thick-client / published HTTP). Not smoke-tested against live ERP.
3. **Headless extension confirmation** — no quick extension-only dump (Cyrillic extension name breaks some CLI args). Confirm in **Конфигуратор → Расширения** that `MCP_Сервер` and `mcp_dev` are applied; F7 update DB if prompted.

## MCP client: `1c-mcp-engine` (current)

Stdio + httppoll (works without IIS; needs 1C agent):

```json
"1c-mcp-engine": {
  "command": "C:\\1C-Lab\\tools\\1c_mcp\\venv\\Scripts\\python.exe",
  "args": ["-m", "src.py_server"],
  "cwd": "C:\\1C-Lab\\tools\\1c_mcp",
  "env": {
    "PYTHONPATH": "C:\\1C-Lab\\tools\\1c_mcp",
    "PYTHONIOENCODING": "utf-8",
    "MCP_TRANSPORT": "httppoll",
    "MCP_HTTPPOLL_HOST": "127.0.0.1",
    "MCP_HTTPPOLL_PORT": "9090",
    "MCP_ONEC_SERVICE_ROOT": "mcp"
  }
}
```

## HTTP-only path (after IIS + web publication)

Direct Cursor HTTP (no Python proxy):

```json
"1c-mcp-engine-http": {
  "url": "http://localhost/<publication>/hs/mcp/"
}
```

Or stdio proxy with `MCP_TRANSPORT=http`, `MCP_ONEC_URL`, `MCP_ONEC_USERNAME`, `MCP_ONEC_PASSWORD` (see `1c_mcp/mcp_client_settings/cursor/mcp.json`).

## Next

- MCP-006 / MCP-007: help MCP, consolidate routing docs.
- After publish: smoke `tools/list` (or equivalent) via `1c-mcp-engine`; switch to `http` transport if preferred.
